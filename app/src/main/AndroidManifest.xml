<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".di.StartKitApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StartKit"
        tools:targetApi="31">

        <activity
            android:name=".ui.features.start.StartActivity"
            android:exported="true"
            android:theme="@style/Theme.StartKit.Splash">
        </activity>

        <activity
            android:name=".ui.features.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.StartKit"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <activity android:name=".ui.navigator.ComposableActivity"
            android:theme="@style/Theme.StartKit"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
